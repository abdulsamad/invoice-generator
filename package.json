{
  "name": "invoice-generator",
  "version": "1.0.0",
  "main": "index.js",
  "author": "Abdul Samad",
  "license": "UNLICENSED",
  "private": true,
  "scripts": {
    "prestart": "rm -rf .cache",
    "start": "parcel src/index.html src/receipt.html",
    "prebuild": "rm -rf dist && rm -rf .cache",
    "build": "parcel build src/index.html src/receipt.html --no-content-hash --no-source-maps --no-minify"
  },
  "devDependencies": {
    "parcel-bundler": "^1.12.5",
    "sass": "^1.35.1",
    "typescript": "^4.3.5"
  },
  "dependencies": {
    "alpinejs": "^3.2.1",
    "bulma": "^0.9.3",
    "jspdf": "^2.3.1"
  }
}
